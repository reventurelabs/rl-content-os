---
name: voice
description: Build VOICE-PROFILE.md from real writing samples (or a structured interview if none exist), validated against a test passage.
argument-hint: '[nothing needed — the skill will ask for writing samples]'
disable-model-invocation: true
license: MIT
---

Invoke the `rl-voice-discovery` skill now. That skill file is the only source of
truth for how samples are gathered, which patterns get extracted, and how the
draft profile is validated — read it and follow it rather than working from
memory.

**Request:** $ARGUMENTS
