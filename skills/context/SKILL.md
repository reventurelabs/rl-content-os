---
name: context
description: Build or refresh AUTHOR-CONTEXT.md through a guided interview — who you write for, why you write, and what's worth publishing.
argument-hint: '[nothing needed — the skill checks for existing evidence, then interviews]'
disable-model-invocation: true
license: MIT
---

Invoke the `rl-context-discovery` skill now. That skill file is the only source
of truth for the evidence check, the interview technique, and the validation
step — read it and follow it rather than working from memory.

**Request:** $ARGUMENTS
